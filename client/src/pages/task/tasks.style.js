import styled from "styled-components";
import {
  NoteContainer,
  NoteWrapper,
  NoteListWrapper,
} from "../note/note.style";

export const TasksContainer = styled(NoteContainer)``;

export const TaskWrapper = styled(NoteWrapper)``;

export const TaskListsWrapper = styled(NoteListWrapper)``;
